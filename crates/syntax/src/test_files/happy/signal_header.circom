{
    // signal header: both keyword orders and (optional) tag lists (tags precede the name).
    signal input a;
    signal output b;
    signal c;
    input signal d;
    output signal e;
    signal {t1} f;
    signal input {t1, t2} g;
}
